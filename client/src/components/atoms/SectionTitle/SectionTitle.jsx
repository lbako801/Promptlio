import React from "react";
import { StyledTitle } from "./SectionTitle.styles";

const SectionTitle = ({ children }) => {
  return <StyledTitle>{children}</StyledTitle>;
};

export default SectionTitle;
